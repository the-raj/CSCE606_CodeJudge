<!DOCTYPE html>
<html>
  <head>
    <title><%= (yield(:title) + " | " unless yield(:title).blank?).to_s + "CodeJudge" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
    <%= javascript_importmap_tags %>
    <%= yield :head %>
  </head>

  <body>
    <div id="login-page" class="layout">
      <div class="container">
        <div class="row">
          <div class="col-12 col-md-6 offset-md-3">
            <%= content_for?(:login_content) ? yield(:login_content) : yield %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
