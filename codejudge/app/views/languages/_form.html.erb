<%= form_with(model: language) do |form| %>
  <% if language.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(language.errors.count, "error") %> prohibited this language from being saved:</h2>

      <ul>
        <% language.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.label "Language Name"  %>
  <%= form.text_field :name %>
  <%= form.label "Extension" %>
  <%= form.text_field :extension %>
  <%= form.label "API URL" %>
  <%= form.text_field :url_name %>

  <div>
    <%= form.submit "Add Language", :class => "button" %>
  </div>
<% end %>
